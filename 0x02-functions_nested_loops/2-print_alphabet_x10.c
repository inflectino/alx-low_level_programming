#include "main.h"
/**
 * main - Alphabets 10 times
 *
 * Return: 0 on success
 */
void print_alphabet_x10(void)
{
	int lc;
	int n;

	while (n > 10)
	do{

                for (lc = 'a'; lc <= 'z'; lc++)
                {
                        _putchar(lc);
                }
                _putchar('\n');
	
	}
}
